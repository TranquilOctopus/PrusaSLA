---
description: Summarize PrusaSLA roadmap progress and what is blocking it
---
Recent commits:
!`git log --oneline -15`

Roadmap:
@doc/sla-fork/ROADMAP.md

Don't change any files. Report:
1. Ticked and total todos for each milestone.
2. Todos that can start now (unticked, not `[human]`, all `needs` ticked).
3. `[human]` todos waiting on a person, ordered by how many other todos they unblock.
4. Todos with a `Blocked:` line, with the reason.
